@page "/marcas/delete/{id:int}"
@using ModelsLibrary.Models
@inject HttpClient Http
@inject NavigationManager Nav

<PageTitle>Excluir Marca — GestãoPro</PageTitle>

<div class="page-header">
    <div>
        <div class="page-title">Excluir Marca</div>
        <div class="page-subtitle">Esta ação não pode ser desfeita</div>
    </div>
    <a href="marcas" class="btn btn-secondary">← Voltar</a>
</div>

@if (marca == null)
{
    <div class="loading-spinner"><div class="spinner"></div> Carregando...</div>
}
else
{
    <div class="card" style="max-width:520px">
        <div class="card-body">
            <div class="alert alert-warning">
                Você está prestes a excluir permanentemente a marca <strong>@marca.Nome</strong>. Deseja continuar?
            </div>
            @if (!string.IsNullOrEmpty(erro))
            {
                <div class="alert alert-danger">@erro</div>
            }
            <div class="d-flex gap-2">
                <button class="btn btn-danger" @onclick="Excluir">Confirmar Exclusão</button>
                <a href="marcas" class="btn btn-secondary">Cancelar</a>
            </div>
        </div>
    </div>
}

@code {
    [Parameter] public int Id { get; set; }
    private Marcas? marca;
    private string erro = string.Empty;

    protected override async Task OnInitializedAsync()
    {
        marca = await Http.GetFromJsonAsync<Marcas>($"api/marcas/{Id}");
    }

    private async Task Excluir()
    {
        erro = string.Empty;
        var resp = await Http.DeleteAsync($"api/marcas/{Id}");
        var resultado = await resp.Content.ReadFromJsonAsync<RespostaApi>();
        if (resp.IsSuccessStatusCode)
            Nav.NavigateTo("marcas");
        else
            erro = resultado?.Mensagem ?? "Erro ao excluir marca.";
    }

    private record RespostaApi(string Mensagem);
}
